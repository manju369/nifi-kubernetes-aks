apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nifi-azure-managed-default-disk
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi
